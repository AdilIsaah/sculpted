import { HorizontalBarGraphDirective } from "./horizontal-bar-graph.directive";

describe("HorizontalBarGraphDirective", () => {
	it("should create an instance", () => {
		const directive = new HorizontalBarGraphDirective();
		expect(directive).toBeTruthy();
	});
});
